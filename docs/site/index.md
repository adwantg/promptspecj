# PromptSpec-J

PromptSpec-J gives Java teams a contract-first workflow for prompts:

- define prompts as versioned YAML or JSON contracts
- validate prompt shape and tool allowlists during the build
- generate typed Java clients
- snapshot prompt behavior for CI drift detection

The first release targets Spring AI teams that want stronger governance without replacing their runtime stack.
